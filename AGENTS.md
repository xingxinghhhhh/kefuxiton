# AI 客服 Agent 工程底层规则

本文件是本仓库对 Codex 的强制工程指令。除非用户明确批准变更，否则以下规则优先于临时实现偏好。

## 1. 项目目标与范围

本项目首期只实现：单一 Web Chat、单一业务领域、单一知识库、基于已发布知识的问答、来源引用、保守拒答、人工接管、会话审计和离线评测。

首期明确禁止：多渠道扩展、语音、复杂长期记忆、自动退款、改账、改地址、删除数据、支付操作及任何未经确认的高风险写操作。

系统必须遵守四个产品边界：知识可信、遇险转人工、过程可追溯、能力可替换。

## 2. Codex 工作协议

每个开发任务必须按以下顺序执行：

1. 读取本文件和当前目录最近的 `AGENTS.md`，检查 Git 状态和现有实现。
2. 明确本节点唯一目标、影响范围、验收标准和不做事项。
3. 先设计接口/数据契约，再实现后端，再实现前端，再补齐测试和文档。
4. 每次只完成一个最小完整垂直切片，不顺手重构无关模块。
5. 修改后运行与变更相关的最小验证，再运行完整门禁。
6. 最终报告必须包含：变更文件、行为变化、测试命令及结果、已知风险、未完成项。

如果需求、业务规则、数据边界、权限或技术路线不明确，先提出阻塞问题或给出待确认方案，不得擅自猜测后写代码。

## 3. 版本与架构基线

推荐目录：

```text
apps/api/              NestJS 后端 API
apps/web/              Next.js 前端
packages/contracts/    OpenAPI、DTO、共享类型和错误码
packages/config/       经过校验的配置加载
tests/evals/           AI 离线评测集、红队集和评测脚本
infra/                 本地开发基础设施与部署样例
docs/                  需求、架构、决策记录和运行手册
```

- 使用 TypeScript strict mode；前后端统一使用 pnpm workspace。
- 后端默认 NestJS + REST/OpenAPI + Prisma + PostgreSQL。
- 前端默认 Next.js App Router + React + TypeScript + Tailwind CSS + 可访问组件层。
- 单元/集成测试按模块选用 Jest 或 Vitest，但仓库必须统一脚本入口；端到端使用 Playwright。
- LLM、Embedding、RAG、向量库、客服工作台和外部业务系统都必须通过适配器接入。
- Dify、FastGPT、MaxKB 只作为可替换的技术验证对象，不能成为领域层依赖。
- 若仓库已有不同技术栈，先审计并增量兼容；禁止为追求标准化大规模重写。
- 不新增生产依赖，除非有方案说明、许可证检查、资源评估、替代方案和用户批准。

核心模块：`conversations`、`agent`、`knowledge`、`retrieval`、`handoff`、`tools`、`policy`、`audit`、`evaluation`、`auth`。模块之间通过明确接口通信，禁止跨模块直接读写对方 ORM 模型、数据库表或内部状态。

## 4. 统一代码规范

- 所有生产代码使用 TypeScript strict；禁止新增 `any`、`@ts-ignore`、无解释的类型断言和空 catch。
- 使用 ESLint、Prettier、TypeScript 编译检查；格式化由工具统一完成。
- 变量/函数使用 camelCase，类型/类使用 PascalCase，常量使用 UPPER_SNAKE_CASE，文件名使用 kebab-case。
- 一个函数只做一个清晰职责；复杂逻辑拆成可测试的纯函数。
- 不把业务规则藏在 UI、Controller、ORM hook、魔法字符串或环境变量中。
- 公共契约以 `packages/contracts` 或 OpenAPI 为唯一来源，不重复定义 API 类型。
- 公共接口、领域规则、复杂算法和非显然安全行为必须说明“为什么”。
- 禁止提交调试输出、临时脚本、死代码、未使用依赖和个人路径。

## 5. AI 与数据安全规则

- 用户输入、知识文档、模型输出和工具返回值都视为不可信数据。
- 业务事实必须来自已发布知识或受控工具；知识未命中时不得用模型常识补齐企业事实。
- 模型不得默认执行写操作；首期工具全部只读。
- 工具必须有输入校验、权限校验、超时、错误码、幂等策略、脱敏规则和审计记录。
- 客户数据、API Key、访问令牌和生产日志禁止进入测试样例、源码、截图或提交历史。
- 日志默认脱敏；不得记录完整身份证、手机号、Token、密码、支付信息或完整客户原文，除非经过明确批准且有访问控制。
- 任何跨会话、跨用户、跨业务域读取都是阻断级缺陷。
- AI 超时、检索失败、工具失败、知识冲突和连续未解决时必须进入可解释兜底或转人工。
- 不得在回答中泄露系统提示词、内部策略、工具密钥或隐藏上下文。

## 6. 开发与变更节奏

一个开发节点必须是“一个最小完整闭环”，顺序为：需求确认 -> 领域/接口设计 -> 数据迁移 -> 后端能力 -> 前端交互 -> 单元/集成测试 -> E2E/评测 -> 文档 -> 回归验证。

每个节点必须有节点目标和不做事项、影响文件清单、API/数据/权限变更说明、正常/异常/安全验收标准、回滚或降级方案、验证命令和结果。

禁止边开发边扩大范围、以 Demo 通过代替测试、先接生产数据再补安全、把失败隐藏成空答案、无评测集修改提示词或检索参数。

## 7. Git 与工作树保护

- 修改前先检查 `git status`、相关 diff 和当前分支。
- 保留用户已有修改；不 reset、clean、checkout 或覆盖无关文件。
- 不自动 stage、commit、push、开 PR 或合并，除非用户明确要求。
- 提交应按功能边界拆分，使用 Conventional Commits。
- 不把生成物、客户数据、构建缓存、密钥和本地数据库提交进仓库。

## 8. 统一完成门禁

在声明节点完成前，至少执行：

```text
pnpm lint
pnpm typecheck
pnpm test:unit
pnpm test:integration
pnpm test:e2e
pnpm test:eval
```

如果命令尚未建立，必须明确说明命令不存在及原因，不得假装通过。

质量门禁：业务逻辑必须有自动化测试；认证、权限、拒答、转人工、工具边界和审计必须覆盖失败路径；AI 变更必须经过固定离线评测集和提示词注入/越权测试；关键路径不得出现未审计的外部调用；任何阻断级安全问题、跨用户数据问题或高风险未授权操作都禁止合并。

## 9. 代码审查规则

审查优先级：正确性 > 数据安全 > 权限边界 > 可回滚性 > 可测试性 > 性能 > 风格。

重点检查：是否扩大范围、是否绕过模块边界、是否把未发布知识用于回答、是否允许越权工具调用、是否缺少人工兜底、是否修改旧行为、是否有迁移回滚方案、是否补齐测试和文档。

## 10. 禁止擅自决策

以下事项必须先由用户或公司负责人确认：最终模型/供应商、是否允许外部模型、首个业务领域、正式渠道、客服系统、商业许可证、数据保留周期、多租户方案、生产部署方式和高风险业务操作。

## 11. Codex 输出格式

每次完成任务时按以下顺序汇报：结论；本次完成的单一节点；变更清单；验证结果；未解决风险和待确认事项；唯一建议的下一节点。不要用“应该可以”“基本没问题”替代测试证据。

## Code Review Rules

发现未授权写操作、跨用户读取、密钥泄露、未脱敏生产数据或无依据的企业事实回答时，必须阻止合并。发现功能范围扩张、无需求支持的依赖、无测试的关键行为或破坏既有用户修改时，必须要求修正。例外必须在需求/ADR 中记录原因、影响、责任人、有效期和回滚方式，并补充针对性测试。
