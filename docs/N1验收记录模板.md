# N1 验收记录

## 节点

- 名称：单 Web Chat 会话安全闭环
- 状态：`N1_ACCEPTED`
- 分支：`feat/n1-web-chat-session`
- 审核结论：ChatGPT 先后批准 N1 最小测试启动修复、Jest 配置修复和 API 构建配置修复；最终运行证据已完成，等待 ChatGPT 对最终证据确认后进入 N2
- 验收日期：2026-08-09

## 变更范围

- API：生产构建明确输出 `dist`，生产入口仍为 `NestFactory.create(AppModule)`
- Web：完整 `/chat` Playwright 闭环
- Contracts：沿用 N1 共享契约
- Prisma/迁移：D 盘 PostgreSQL 16 空库迁移
- 测试/评测：真实 PostgreSQL 集成测试、生产 API smoke、Jest 相对导入映射、完整 Playwright、AI 评测

## 验证环境

- PostgreSQL 程序与数据目录：`D:\AI\runtime\postgresql16`
- PostgreSQL 服务：`postgresql-x64-16-d`，Running，Automatic
- Playwright 浏览器目录：`D:\AI\playwright-browsers`
- 项目数据库：`ai_customer_service`
- 迁移：从空库成功执行 `0001_init`
- C 盘 PostgreSQL 残留：已清理，未在 C 盘保留 PostgreSQL 安装目录

## 验证证据

| 检查项 | 命令/证据 | 结果 |
| --- | --- | --- |
| 项目规则 | `pnpm verify` | 通过，22 个基础文件和 8 个角色文件 |
| Lint | `pnpm lint` | 通过 |
| 类型 | `pnpm typecheck` | 通过 |
| 单元测试 | `pnpm test:unit` | 通过，4 套件 / 5 测试 |
| 服务边界测试 | fake-Prisma 测试 | 通过 |
| 空 PostgreSQL 迁移 | `DATABASE_URL=... pnpm --filter @ai-agent/api db:migrate` | 通过，空库应用 `0001_init` |
| Prisma schema | `DATABASE_URL=... pnpm --filter @ai-agent/api db:validate` | 通过 |
| 真实数据库集成 | `RUN_REAL_DB_TESTS=1 pnpm test:integration` | 通过，1 套件 / 1 测试，未 skip |
| 真实生产 API smoke | `pnpm test:api-smoke` | 通过：health、创建会话、发送消息、错误凭证、进程重启后继续使用原会话 |
| 完整 Playwright E2E | `pnpm test:e2e` | 通过，1 测试 / 1 通过；覆盖 `/chat`、空消息禁用、创建会话、发送消息、Mock Agent 响应和 `safe_unavailable` |
| AI 评测 | `pnpm test:eval` | 通过，2 个确定性样例 |
| 构建 | `pnpm build` | 通过；存在 Next.js ESLint plugin 非阻断 warning |
| API 构建产物 | `pnpm --filter @ai-agent/api build` | 生成 `apps/api/dist/main.js` |
| 跨会话隔离 | 真实集成测试 + API smoke | A 凭证不能操作 B；错误凭证返回 401 |
| 持久化与重启 | 真实集成测试 + API smoke | 用户/Agent 消息真实写入，重启后原凭证仍可继续发送 |
| 日志与模型边界 | 代码检查、Mock Agent、运行验证 | 无真实模型、RAG、外部业务调用或凭证日志 |

## N1 修复记录

- 真实集成测试显式使用 `ExpressAdapter`，绕过 Nest 默认适配器加载与 Jest 运行时解析冲突。
- Jest 的 `.js` 映射仅匹配 `./` 和 `../` 相对导入，不再错误改写 `ipaddr.js` 等第三方包名。
- API `tsconfig` 明确 `noEmit: false`，使 `nest build` 生成可启动的 `dist`；根级 `noEmit: true` 保持不变。
- 新增 `pnpm test:api-smoke`，通过生产入口进程执行真实 HTTP 和重启持久化验证。

## 未实现与风险

- N1 仍不包含真实模型、知识库、RAG、引用生成、后台、RBAC、多租户或业务写操作。
- `pnpm build` 的 Next.js ESLint plugin warning 和开发服务器的 module type/cross-origin warning 均为非阻断项，需后续独立清理。
- `N1_ACCEPTED` 只表示本地 N1 运行证据已完成；进入 N2 前仍需将最终证据提交 ChatGPT 并得到确认。

## ChatGPT 最终确认前的下一动作

将本记录、分支状态、提交 hash、完整门禁结果和剩余 warning 发送给 ChatGPT，请其确认 N1 最终验收；确认前不开始 N2。
