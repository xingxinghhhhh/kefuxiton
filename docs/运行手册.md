# 运行手册：配置与安全预检

## 本地 synthetic/test 运行

1. 复制 `.env.example` 到本地环境文件，并按本机 PostgreSQL 调整；不得提交真实凭证。
2. 运行 `pnpm --filter @ai-agent/api db:migrate` 应用迁移。
3. 需要 Staff E2E 时使用仅限测试的 `STAFF_AUTH_MODE=test` 和合成 Token；其他本地运行保持 `STAFF_AUTH_MODE=deny`。
4. 启动前运行 `pnpm preflight`。
5. 通过后再运行 `pnpm dev`、测试或构建。

`pnpm preflight` 只验证配置格式和运行边界，不连接数据库、不迁移、不发布知识、不调用模型或外部系统。

退出码 `0` 表示通过，`1` 表示配置缺失/非法或环境禁止组合，`2` 表示预检程序自身异常；三种情况都不会打印配置值。

## 配置字段

| 字段 | 作用 | 安全边界 |
| --- | --- | --- |
| `APP_ENV` | `development`、`test` 或 `production` | production 禁止测试能力 |
| `PORT` | API 监听端口 | 1–65535 |
| `WEB_ORIGIN` | 允许的单一 Web Origin | production 必须 HTTPS |
| `DATABASE_URL` | PostgreSQL 连接 URL | 只校验结构，错误不回显值 |
| `NEXT_PUBLIC_API_BASE_URL` | Web 使用的 API base URL | 不允许用户名、密码、查询或片段 |
| `STAFF_AUTH_MODE` | `deny` 或 `test` | production 只能 deny，正式身份尚未接入 |
| `AI_AGENT_TEST_STAFF_TOKEN` | 测试身份凭证 | 仅 test 模式使用，禁止提交和输出 |
| `ALLOW_KNOWLEDGE_PUBLISH` | 受控知识发布开关 | production 必须为 `0` |

## 失败处理

预检失败时只报告 `CONFIG_MISSING`、`CONFIG_INVALID`、`CONFIG_FORBIDDEN_IN_ENV`、`CONFIG_SECRET_FORBIDDEN` 或 `CONFIG_URL_INVALID` 及字段名。API 配置失败时不监听端口；不要把完整数据库 URL、密码、Token、API Key、Prisma 原始异常或客户消息复制到日志。

## 故障排查

- `CONFIG_MISSING`：补齐 `.env.example` 中对应字段后重新运行预检。
- `CONFIG_URL_INVALID`：确认 URL 协议、主机、路径和无凭证要求；不要把密码粘贴到聊天或提交记录。
- `CONFIG_FORBIDDEN_IN_ENV`：production 下关闭测试身份或知识发布；不要通过删除校验绕过。
- API 未监听：先运行 `pnpm preflight`，再检查安全错误码；不要直接启动旧构建产物。

## 生产前未完成项

## N18 LangGraph Human-in-the-loop 实验

N18 仅在 `packages/langgraph-lab` 中运行，不提供生产 API 或 Web 入口。它使用进程内 `MemorySaver`、固定 synthetic `thread_id` 和 `Command({ resume })`，验证“转人工后暂停、人工决策后继续”的状态闭环。

运行实验门禁：

```text
pnpm test:langgraph-lab
```

## N31 合成准入示例与生产门禁一致性回归

N31 只读对照 N30 合成示例、N29 正式准入待确认模板和现有 `release:readiness` CLI。它验证 N30 的 `SYNTHETIC_ONLY`/`NOT_READY`、N29 的 `PENDING_CONFIRMATION`/`NOT_READY`、三种运行目标状态及 production 六个阻断原因的固定顺序，不复制或修改准入逻辑。

固定评测共 14 条：N30 合同 6 条、N29 合同 3 条、三种 readiness 模式 3 条、阻断顺序 1 条、脱敏和文件边界 1 条。评测只输出固定 case 状态和原因码，不输出路径、环境值、源码、正文或堆栈。

运行命令：

```text
node tests/evals/run-n31-eval.mjs
pnpm test:langgraph-lab
```

N31 不改变 N29/N30、production readiness、生产 API/Web、数据库、依赖或部署行为。

## N32 生产门禁不可绕过与 rehearsal 隔离安全回归

N32 是只读 synthetic 安全回归，验证 production readiness 在 NestFactory、Prisma 初始化和端口监听之前 fail-closed；test Staff、知识发布开关和普通环境变量不能绕过 production 门禁；失败输出不泄露 Token、数据库连接串、路径、堆栈或客户内容。它不需要真实业务、模型、身份、部署、保留策略或任何生产授权。

固定 N32 评测共 12 条：production readiness 3 条、启动边界 2 条、绕过抵抗 3 条、脱敏 2 条、local_eval/rehearsal 运行模式 2 条。评测只输出 `caseId`、`status`、`reasonCode`，并复用现有 `release:readiness` CLI；生产源码和准入 evaluator 不复制、不修改。

运行命令：

```text
node tests/smoke/n32-production-boundary.mjs --case=production-ready-gate
node tests/evals/run-n32-eval.mjs
```

N32 必须保持 `local_eval=LOCAL_EVAL_READY`、`rehearsal=REHEARSAL_READY`、`production=NOT_READY`。rehearsal 继续使用 `APP_ENV=rehearsal`、deny-by-default Staff 和既有 schema/进程/端口清理机制；production 失败时不得监听端口。N32 不改变 N29–N31、production readiness、生产 API/Web、数据库、权限、依赖、部署或外部服务行为。

## N33 合成/rehearsal 安全证据最终封存

N33 只冻结一个测试/文档内部的 `n33.v1` manifest，汇总 N29–N32 的节点顺序、验收状态、评测数量、三种 readiness 和 production blocker 顺序。manifest 不包含原始文档、完整评测输出、路径、环境变量、Token、客户内容，也不进入 `packages/contracts` 或生产审计存储。

固定根字段顺序为 `schemaVersion`、`evidenceType`、`nodeOrder`、`expectedNodeCount`、`items`、`readiness`、`productionBlockers`、`syntheticOnly`、`productionIntegration`、`manifestDigest`。`manifestDigest` 是排除自身后的固定字段顺序紧凑 UTF-8 JSON 的小写 SHA-256。节点顺序固定为 N29/N30/N31/N32，评测数量为 15/14/14/12，`syntheticOnly=true`，`productionIntegration=false`。

N33 固定评测共 16 条：节点摘要 4 条、readiness 3 条、blocker 顺序 1 条、安全边界 3 条、manifest 完整性 2 条、篡改/缺失/未知字段 fail-closed 2 条、既有入口接入 1 条。评测只输出 `caseId`、`status`、`reasonCode`。运行命令：

```text
node tests/evals/run-n33-eval.mjs
```

N33 不改变 N29–N32、production readiness、生产 API/Web、数据库、权限、依赖、部署或外部服务行为；生产继续 `NOT_READY`，local_eval/rehearsal 状态不变，也不生成持久化证据库。

## N33-R1 同 Schema API smoke/E2E 状态隔离

N33-R1 修复 synthetic release rehearsal 中 API smoke 完成后污染同一 schema 的 E2E 会话、接管、人工回复、审计、反馈和内部上下文的问题。`tests/smoke/api-production.mjs` 只跟踪本次创建的 conversation，并按现有外键依赖顺序清理其 message feedback、operator reply、internal note、conversation tag、audit event、message、handoff request 和 conversation；published synthetic knowledge fixture 保留。清理不使用 `TRUNCATE`、`migrate reset` 或无范围删除。

`scripts/run-release-rehearsal.mjs` 在 E2E 前比较 API smoke 前后的无内容业务关系摘要；摘要不一致或清理失败即失败。演练继续在同一个 `release_rehearsal_*` schema 中按 `API smoke → E2E` 顺序执行，不能改成独立 schema、跳过 E2E 或放宽断言。

N33-R1 仅修改 test-only harness 和文档，不修改生产 API/Web、数据库模型、权限、readiness、依赖或 N33 manifest/eval。验证要求连续三次运行：

```text
pnpm test:release-rehearsal
pnpm test:release-rehearsal
pnpm test:release-rehearsal
```

每次必须包含 `api_smoke_cleanup=passed`、同 Schema E2E 3/3、rollback validation、schema cleanup 和 baseline worktree cleanup。

## N34 合成验证主线收口与生产冻结

N34 只新增测试/文档内部的 `n34.v1` 最终冻结摘要，封存链路 `N29 → N30 → N31 → N32 → N33 → N33-R1`。它只证明 synthetic/local_eval/rehearsal 证据链完整，不代表生产授权、上线批准或真实业务确认。

Manifest 的根字段和 item 字段顺序固定，`evidenceDigest` 使用最小安全摘要对象的紧凑 UTF-8 JSON 小写 SHA-256，`manifestDigest` 使用排除自身后的完整 manifest 紧凑 UTF-8 JSON 小写 SHA-256。摘要不保存原始日志、客户内容、Token、连接串、绝对路径、Prompt、堆栈、端口或环境值；Manifest 上限为 12 KiB。

N34 固定 18 条评测，运行：

```text
node --check tests/evals/run-n34-eval.mjs
node tests/evals/run-n34-eval.mjs
```

评测成功只输出 `caseId`、`status`、`reasonCode`。N33-R1 的冻结摘要仅为三次 rehearsal、API smoke cleanup、同 Schema E2E 3/3、rollback/schema/worktree cleanup 全部通过。现有 N33-R1 证据仍需连续执行：

```text
pnpm test:release-rehearsal
pnpm test:release-rehearsal
pnpm test:release-rehearsal
```

每次演练后必须清理临时 Schema、端口、进程、detached worktree 和生成物。readiness 继续保持 `local_eval=LOCAL_EVAL_READY`、`rehearsal=REHEARSAL_READY`、`production=NOT_READY`；生产冻结状态为 `FROZEN`，授权为 `NOT_GRANTED`，真实输入为 `NOT_AVAILABLE`。N34 不修改生产 API、Web、数据库、权限、readiness、依赖或锁文件。

## N29 生产准入待确认模板冻结

N29 只记录生产接入前仍待负责人确认的七组决策，不填写真实业务、知识、模型、身份、部署、保留或回滚值。模板位于 `docs/templates/N29-生产准入待确认模板.md`，所有引用保持 `<PENDING>`，`confirmationStatus` 固定为 `PENDING_CONFIRMATION`，`productionReadiness` 固定为 `NOT_READY`；`contentSha256` 和 `canonicalSha256` 在正式来源批准前保持待计算状态。

N29 不修改 N15/N28 readiness evaluator，不打开 `ALLOW_KNOWLEDGE_PUBLISH`，不启用正式 Staff Identity 或 Agent Provider，也不接入 API、Web、数据库、模型、部署或外部系统。生产 readiness 继续返回既有阻断原因，`local_eval` 与 `rehearsal` 行为不变。

固定 N29 评测共 15 条：模板字段/占位符 5 条、七组待确认决策 7 条、生产门禁 1 条、local_eval 1 条、rehearsal 1 条。评测命令：

```text
pnpm test:langgraph-lab
pnpm test:eval
pnpm release:readiness --target=local_eval
pnpm release:readiness --target=rehearsal
pnpm release:readiness --target=production
```

生产目标必须继续以非零退出返回 `NOT_READY`。N29 回滚点为 `fc3d7ab`，不涉及数据库或生产回滚。

## N28 LangGraph 实验层封存与生产接入前决策包

N28 是只读、非生产的封存节点。它汇总 N17–N27 的离线证据，复用现有 N15 readiness evaluator，固定当前生产状态为 `NOT_READY`，并记录正式业务输入、知识来源、模型/Provider、Staff Identity、部署目标、数据保留和回滚负责人等待确认事项。`local_eval` 和 `rehearsal` 分别保持 `LOCAL_EVAL_READY` 与 `REHEARSAL_READY`；任何 synthetic、deny/test Staff 或 deterministic Agent 都不能被解释为生产就绪。

N28 还静态核对 `apps/api`、`apps/web`、`packages/config`、`packages/contracts` 的源码和 package manifest 未依赖 `packages/langgraph-lab`、LangGraph、LangChain 或 `MemorySaver`，且根生产 `build` 不构建实验包。N25 的 `EvidenceGateSnapshot` 仍为固定九字段，N27 仍为 18 条评测；N28 不新增 `n28.v1`、公共契约、生产 API、数据库、依赖、环境变量、外部调用或持久化。

固定 N28 评测共 21 条：readiness 10 条、生产依赖/构建隔离 5 条、N25/N27 契约保持 3 条、敏感信息和外部副作用边界 3 条。评测输出只允许 `caseId`、`status`、`reasonCode`，运行命令为：

```text
pnpm test:langgraph-lab
pnpm test:eval
pnpm release:readiness --target=local_eval
pnpm release:readiness --target=rehearsal
pnpm release:readiness --target=production
```

生产 readiness 命令必须以非零退出并返回 `NOT_READY`；N28 回滚点为 `9897f76`，不涉及数据库或生产操作。

暂停结果不是 `AgentResult`，而是受限的 `n18.v1` 人工审核 payload；只有恢复完成后才产生兼容的五字段结果。人工拒绝、缺失决策、非法决策、重复恢复和过期线程都必须 fail closed。实验不接入数据库、真实身份、客户数据、真实模型、LangSmith、LangGraph Server 或任何外部系统，不得把 N18 `MemorySaver` 当成生产恢复能力。

## N19 LangGraph 执行轨迹实验

N19 在同一实验包内提供显式的 N17/N18 traced runner。默认 N17 图仍无 checkpointer，默认 N18 会话仍只使用进程内 `MemorySaver`；只有调用方明确选择 traced runner 时才注入内存 `TraceRecorder`。

轨迹使用 `n19.v1` 固定结构，只记录节点生命周期、路由、暂停/恢复状态、终态摘要、固定错误码和连续序号。`TracePath` 由真实节点进入事件推导，不能把 `GraphState.path` 或完整 LangGraph 状态直接序列化。轨迹不得包含原始用户输入、完整 interrupt payload、Token、Operator 身份、提示词、隐藏策略、模型输出、数据库对象、路径或 checkpoint 信息。

同一线程恢复使用 `resume` phase；节点重放只记录该 phase 的一次进入/完成事件。重复或过期恢复在调用图前记录 `resume_rejected`，不生成新的终态事件。N19 结果是实验层 `ExecutionTrace`，不等于生产 `AuditEvent`，不写数据库，也不提供生产恢复能力。

## N20 LangGraph 轨迹回放实验

N20 使用测试目录中的固定 synthetic fixture registry，通过 allowlisted `caseId` 选择场景，并将 N17/N18 traced runner 连续执行两次。每次执行都使用独立的 `TraceRecorder`；N18 额外使用独立的进程内 `MemorySaver` 和 synthetic thread。回放只比较固定的 N19 安全摘要与第二次运行结果，不根据脱敏轨迹反推原始业务输入。

回放请求仅允许 `n20.v1`、固定 `caseId` 和 `fixtureVersion`。回放结果只包含事件类型、节点路径、路由、状态、终态、错误码、数量和首个 mismatch 信息；不得输出原始输入、知识正文、人工决策、interrupt payload、trace/thread 标识、checkpoint、端口对象、路径、环境变量或异常堆栈。未知 fixture、版本/hash 错误、轨迹不一致和非确定性均 fail closed。

N20 仍是非生产实验：不提供 API/Web/UI，不写数据库或生产审计，不做跨进程恢复、实时监控、真实模型、外部网络或多租户回放。
## 运行时健康检查（N16）

API 成功启动后提供三个只读检查：

- `GET /api/v1/health`：保留的兼容响应 `{ status: "ok", service: "api" }`。
- `GET /api/v1/health/live`：只证明已启动进程能够响应，不访问数据库。
- `GET /api/v1/health/ready`：执行固定 1000ms 超时的只读 `SELECT 1`；数据库可用返回 `200`，失败返回 `503` 和脱敏 reason code。

健康响应均设置 `Cache-Control: no-store`，不需要客户或 Staff 鉴权，不返回连接串、凭据、堆栈、本地路径、客户数据或生产准入细节。数据库在启动阶段不可用时，API 按既有生命周期不监听端口；此时不存在可访问的 liveness，不能将其解释为运行中。

健康 readiness 只表示运行依赖可用，不代表 production readiness。生产准入仍以 `pnpm release:readiness --target=production` 和 API 启动前 N15 门禁为准。

本仓库当前不执行真实部署、真实模型、企业知识、正式身份或外部系统接入。正式环境必须重新确认 HTTPS、Origin、数据库 SSL、身份适配器和凭证保管方式。

## 合成发布演练与回滚（N12）

在本地 PostgreSQL 上运行：

```text
$env:DATABASE_URL='postgresql://<local-user>:<local-password>@localhost:5432/<local-db>?schema=public'
pnpm test:release-rehearsal
```

脚本会动态选择 API/Web 端口；也可通过 `REHEARSAL_API_PORT` 和 `REHEARSAL_WEB_PORT` 显式指定空闲端口。不要把密码、Token 或连接串作为命令行参数传入。演练只创建并清理唯一的 `release_rehearsal_*` schema；不得指向共享生产库。

阶段顺序固定为：输入/端口校验、schema 创建、8 个迁移一次、合成 published fixture、production preflight/build、生产形态 smoke、复用 API smoke 与 E2E、敏感信息安全摘要、N11 基线兼容回滚读取、子进程终止和 schema 清理。输出为单行 JSON，只允许阶段、状态、构建/迁移摘要、健康结果、哈希引用和错误码；不得输出 DATABASE_URL、密码、Token、客户正文、完整堆栈或本地路径。

退出码：`0` 全部通过，`1` 阶段失败，`2` 输入/配置无效，`3` 超时，`4` 子进程终止失败，`5` schema 清理失败。清理失败必须视为失败，不能继续报告成功。

## N21 LangGraph 回放差异诊断实验

N21 只对已经生成的 N20 `ReplayResult` 做纯内存诊断，不重新执行图、不读取 fixture、不创建 `MemorySaver`，也不访问数据库、网络、模型或生产模块。入口为 `diagnoseReplayResult(result)`，输出固定 `n21.v1` 报告。

报告只保留固定的 case/version、诊断状态、诊断类型、原因码、比较基准、安全摘要、确定性摘要和首个安全 mismatch。请求拒绝、fixture 拒绝、敏感输出拒绝和内部失败均为 fail closed；未知字段、未知枚举、超长数组、超范围数字或异常对象不会被回显。报告最多包含 32 个路径项，索引和计数限制为 0–128，序列化结果不得超过 16 KiB。

固定评测共 21 条：13 个 N20 正常 case，加上 golden mismatch、non-deterministic、request rejected、unknown/version/hash 三类 fixture rejected、sensitive output rejected 和 internal failure。N21 仍是非生产实验，不提供 API、Web、UI、持久化、监控、告警、跨进程恢复或多租户能力。
## N24 LangGraph 非生产多 Case 证据包确定性与版本兼容

N24 不新增证据格式，只复核并固化现有 `n23.v1 EvidenceBundle` 的确定性、输入突变隔离和版本兼容矩阵。相同语义的有效 N22 envelope 必须产生完全相同的 bundle 和 SHA-256；对象属性插入顺序不能影响结果，输入数组不会被静默排序。

封存后修改调用方 envelope 或输入数组不得改变已生成 bundle；验证不得修改调用方对象，重复验证结果必须一致。当前 N23 版本、未知版本、source version、digest algorithm、envelope 结构、envelope/item digest 和 bundle digest 的行为继续使用既有 reason code，N24 不新增 `n24.v1`、不做静默迁移或未来版本兼容。

固定 N24 评测为 16 条，并保留 N23 的 13 条评测和 N22 回归。N24 仍不写文件、不入库、不接 API/Web/真实模型/网络，不重新运行回放，不读取 fixture，也不创建 `MemorySaver`。

## N23 LangGraph 非生产多 Case 证据包

N23 将 N20 固定顺序的 13 个业务 replay case 对应的有效 `n22.v1 EvidenceEnvelope` 组织为纯内存 `n23.v1 EvidenceBundle`。包内只保存每个 case 的 `caseId` 和 `evidenceDigest`，不保存完整报告或 envelope；完整 envelope 必须由调用方独立提供给封存/验证函数。

N23 的包字段顺序固定为 `schemaVersion`、`bundleType`、`sourceSchemaVersion`、`digestAlgorithm`、`caseCount`、`items`、`bundleDigest`。canonical manifest 使用紧凑 JSON、UTF-8 和小写 SHA-256，最大 8 KiB；items 不动态排序、去重或自动修正。验证会复用 N22 envelope 校验，并拒绝缺失、重复、未知 case、顺序错误、caseId 不一致、版本/算法错误、envelope 篡改和包 digest 篡改。

验证结果只包含固定 schema、状态、reason code、包 digest 匹配状态和已验证数量，不回显报告正文、输入、人工决策、interrupt payload、Token、路径、堆栈或运行时对象。N23 不写文件、不入库、不接 API/Web/真实模型/网络，不重新运行回放，也不创建 `MemorySaver`。

运行 N23 及此前 LangGraph 离线实验：

```text
pnpm test:langgraph-lab
```

## N22 LangGraph 回放诊断证据封存实验

N22 将已通过严格校验的 N21 `ReplayDiagnosticReport` 封装为纯内存 `n22.v1 EvidenceEnvelope`。封存对象只包含固定版本、证据类型、源版本、`sha256` 算法、规范化报告和小写 64 位摘要指纹；摘要覆盖去除 `evidenceDigest` 后的完整规范化 JSON。字段顺序固定，JSON 无空白，保留 `null`，数组顺序不排序。

入口为 `sealReplayDiagnosticReport(report)` 和 `verifyEvidenceEnvelope(envelope)`。验证只返回固定状态、原因码和 `digestMatch`，不返回报告正文、原始输入、人工决策、interrupt payload、路径、Token、异常堆栈或运行时对象。未知字段、版本错误、摘要篡改、敏感值和超限数据均 fail closed。

该封存只存在于内存和离线评测对象中，不写文件、数据库或生产审计，不重新执行 LangGraph、不读取 fixture、不创建 `MemorySaver`，也不提供 API、Web、UI、监控、告警或跨进程恢复。固定评测共 26 条：21 条合法报告封存/验证，加上摘要篡改、源版本错误、未知字段、敏感值和超限 envelope。
## N25 LangGraph 非生产证据包只读汇总与完整性门禁

N25 只在 `packages/langgraph-lab` 内对已经由 N23 `verifyEvidenceBundle` 验证的 `n23.v1` 证据包生成固定九字段 `EvidenceGateSnapshot`。它只输出门禁状态、固定版本/算法、验证计数、已有 bundle digest、digest 匹配状态和 N23 reason code，不输出 `caseId`、报告、envelope、客户原文、prompt、路径、Token、堆栈或运行时对象。

`buildEvidenceGateSnapshot(bundle, envelopes)` 不重新执行 N23 的 case、顺序、版本、算法或 digest 规则；N23 验证结果是唯一完整性真相。合法完整包返回 `passed/NONE`，其余结果 `blocked`；未知输入、验证器异常或不安全结构统一 fail closed 为 `BUNDLE_INVALID`。N25 不新增 `n25.v1`、HTTP/API、Web、数据库、文件、模型、网络、`MemorySaver` 或生产能力。

运行 LangGraph 离线门禁：

```text
pnpm test:langgraph-lab
```
## N26 LangGraph 非生产证据门禁快照回放与版本回归

N26 只在测试目录使用 14 个固定、受控、内存中的 N25 输入场景。每个场景从全新对象连续调用 `buildEvidenceGateSnapshot` 两次，比较九字段快照和显式字段顺序的紧凑 JSON 字节；不重新执行 N20/N21，不运行 LangGraph，不读取外部 fixture，也不写入文件、数据库或生产系统。

另有 8 条版本与实现回归，固定当前/未知/未来 schema、source/algorithm 映射、N23 14 个 reason code、N25 exact-key 和异常 `BUNDLE_INVALID` fail closed。共 22 条 N26 固定评测，回放结果只包含固定场景 ID、两次 N25 快照、确定性状态和回归状态。

运行离线门禁：

```text
pnpm test:langgraph-lab
```

## N27 LangGraph 非生产证据链一致性与离线准入快照

N27 只在 `packages/langgraph-lab` 的测试目录中验证固定 N20 case 身份与顺序、N22 `evidenceDigest`、N23 item 与 `bundleDigest`、N25 九字段快照以及 N26 两次 fresh replay 的一致性。它继续复用既有 `verifyEvidenceEnvelope`、`sealEvidenceBundle`、`verifyEvidenceBundle` 和 `buildEvidenceGateSnapshot`，不新增运行时 schema 或生产能力。

N27 的固定评测共 18 条：13 条 case 链路、1 条合法 replay 对照和 4 条 digest/顺序篡改。测试输入只允许内存中的 synthetic 数据；不得执行 LangGraph、N20/N21 runner，不得创建 `MemorySaver`，不得读取外部 fixture、数据库、网络、模型或生产模块。运行命令：

```text
pnpm test:langgraph-lab
```
# N35 合成知识包三态导入与检索隔离

N35 只验证合成数据的三态边界：`local_eval` 可导入但不可检索，`rehearsal` 在隔离 Schema 中可发布、回答和引用，`production` 在 readiness、数据库写入和端口监听前拒绝。它不改变生产准入结论，production 仍为 `NOT_READY`。

在配置仅用于本地测试的 `DATABASE_URL` 后执行：

```text
node --check tests/smoke/n35-synthetic-knowledge-modes.mjs
node --check tests/evals/run-n35-eval.mjs
node tests/evals/run-n35-eval.mjs
```

Smoke 会创建并最终删除唯一的 `n35_synthetic_modes_*` Schema，所有失败只输出固定错误码。它复用既有知识导入器、迁移和 API 生命周期，不接生产知识、模型、员工身份或外部系统。
# N36 非生产主线封存与生产冻结维护

N36 只读校验 N34 的最终冻结 Manifest，并回归 N35 三态评测和三种 readiness。它不生成新的持久化证据，不修改生产能力；production 必须保持 `NOT_READY`。

在配置仅用于本地测试的 `DATABASE_URL` 后执行：

```text
node --check tests/evals/run-n36-eval.mjs
node tests/evals/run-n36-eval.mjs
```

N36 成功输出只包含固定 caseId、状态和 reasonCode；不会转发 readiness 或 N35 子进程的原始输出。完整门禁与 N36 的资源清理要求见 [N36-acceptance.md](N36-acceptance.md)。
# N37 非生产冻结基线定期回归

N37 只读核对 N36 提交中的 N34 Manifest、N35/N36 评测基线和三种 readiness，防止非生产冻结证据漂移。它在固定 `2b8a473` detached worktree 中托管未修改的 N36 evaluator，并将依赖视图、Prisma 生成物和构建产物限制在临时 worktree；结束后自动清理，不修改当前工作树源码或依赖链接。它不新增业务功能，production 继续为 `NOT_READY`。

在配置仅用于本地测试的 `DATABASE_URL` 后执行：

```text
node --check tests/evals/run-n37-freeze-regression.mjs
node tests/evals/run-n37-freeze-regression.mjs
```

完整门禁和回滚说明见 [N37-acceptance.md](N37-acceptance.md)。

## N38 冻结基线回归确定性与漂移门禁

N38 每次从固定 `2b8a473` 创建独立临时本地 Git Clone，在 Clone 中连续两次运行未修改的 N37 evaluator，比较固定字段安全摘要，确认 N36 15/15、基线版本、文件边界和清理结果稳定一致。独立 Clone 用于避免 Windows Git 的嵌套 worktree 清理冲突；N37 仍必须在 Clone 内创建并成功清理自己的内部 worktree。N38 不修改生产 API/Web、readiness、Prisma、权限、依赖或 N34–N37 契约；子进程原始输出、路径、密钥和敏感数据不转发，production 继续为 `NOT_READY`。

在配置仅用于本地测试的 `DATABASE_URL` 后执行：

```text
node tests/evals/run-n38-freeze-determinism.mjs
```

完整门禁和回滚说明见 [N38-acceptance.md](N38-acceptance.md)。
